<%= div_for comment, :class => cycle(:odd, :even) do %>
  <div class="author">
    <div class="avatar"><%= gravatar_image_tag comment.email %></div>
    <div class="meta">
      <span class="name"><%= comment_author(comment.author, comment.url) %></span><br/>
      <span class="date">Pastado <%= time_ago_in_words comment.created_at %> atrás</span>
    </div>
  </div>

  <div class="body" style="margin-bottom:20px;">
    <%= comment.body %>
  </div>
<% end %>

